@extends('layouts.app') @section('title','Shipping — Admin') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Shipping zones</h1><form method="post" action="{{route('admin.shipping.store')}}" class="mt-8 grid gap-3 rounded-2xl border bg-white p-6 md:grid-cols-3">@csrf<input name="name" placeholder="Zone name" class="rounded-xl border p-3"><input name="states" placeholder="States, comma separated" class="rounded-xl border p-3"><input name="rate" type="number" step="0.01" placeholder="Rate" class="rounded-xl border p-3"><input name="free_shipping_minimum" type="number" step="0.01" placeholder="Free shipping minimum" class="rounded-xl border p-3"><input name="estimated_days" type="number" value="3" class="rounded-xl border p-3"><button class="rounded-xl bg-blue-600 p-3 font-bold text-white">Create zone</button></form><div class="mt-8 grid gap-4 md:grid-cols-3">@foreach($zones as $z)<div class="rounded-2xl border bg-white p-5"><b>{{$z->name}}</b><p class="mt-2 text-sm">₦{{number_format($z->rate,2)}} · {{$z->estimated_days}} days</p><p class="text-xs text-slate-500">{{implode(', ',$z->states??[])}}</p></div>@endforeach</div></section>@endsection
