@extends('layouts.app') @section('title','Vendor payouts — Admin') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Vendor payouts</h1><div class="mt-8 space-y-3">@foreach($payouts as $p)<form method="post" action="{{route('admin.payouts.update',$p)}}" class="grid gap-3 rounded-xl border bg-white p-5 md:grid-cols-5">@csrf @method('PATCH')<span><b>{{$p->vendor->business_name}}</b></span><span>₦{{number_format($p->amount,2)}}</span><span>{{$p->account_number}}</span><select name="status" class="rounded-lg border p-2">@foreach(['requested','approved','paid','rejected'] as $s)<option @selected($p->status===$s)>{{$s}}</option>@endforeach</select><button class="rounded-lg bg-slate-900 font-bold text-white">Save</button></form>@endforeach</div>{{$payouts->links()}}</section>@endsection
