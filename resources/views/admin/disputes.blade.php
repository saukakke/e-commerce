@extends('layouts.app') @section('title','Disputes — Admin') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Disputes</h1><div class="mt-8 space-y-3">@foreach($disputes as $d)<form method="post" action="{{route('admin.disputes.update',$d)}}" class="rounded-2xl border bg-white p-5">@csrf @method('PATCH')<b>{{$d->subject}}</b><p class="text-sm">{{$d->description}}</p><div class="mt-3 flex gap-2"><select name="status" class="rounded-lg border p-2">@foreach(['open','in_review','resolved','rejected'] as $s)<option @selected($d->status===$s)>{{$s}}</option>@endforeach</select><input name="resolution" value="{{$d->resolution}}" placeholder="Resolution" class="flex-1 rounded-lg border p-2"><button class="rounded-lg bg-slate-900 px-4 font-bold text-white">Save</button></div></form>@endforeach</div>{{$disputes->links()}}</section>@endsection
