@extends('layouts.app') @section('title','Categories — Shoply') @section('content')<section class="mx-auto max-w-5xl px-4 py-12"><h1 class="text-4xl font-black">Categories</h1><form method="post" action="{{route('admin.categories.store')}}" class="mt-8 flex flex-col gap-3 rounded-2xl border bg-white p-6 md:flex-row">@csrf<input name="name" placeholder="Category name" class="flex-1 rounded-xl border px-4 py-3"><input name="description" placeholder="Description" class="flex-1 rounded-xl border px-4 py-3"><button class="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Add</button></form><div class="mt-6 space-y-2">@foreach($categories as $category)<div class="flex justify-between rounded-xl border bg-white p-4"><span><b>{{$category->name}}</b><small class="ml-2 text-slate-400">{{$category->products_count}} products</small></span><form method="post" action="{{route('admin.categories.delete',$category)}}">@csrf @method('DELETE')<button class="text-sm font-bold text-red-600">Delete</button></form></div>@endforeach</div></section>@endsection
