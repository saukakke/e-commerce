@extends('layouts.app') @section('title','Campaigns — Admin') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Promotion campaigns</h1><form method="post" action="{{route('admin.campaigns.store')}}" class="mt-8 grid gap-3 rounded-2xl border bg-white p-6 md:grid-cols-3">@csrf<input name="name" placeholder="Campaign name" class="rounded-xl border p-3"><select name="type" class="rounded-xl border p-3"><option>percent</option><option>fixed</option><option>free_shipping</option></select><input name="value" type="number" step="0.01" placeholder="Value" class="rounded-xl border p-3"><input name="starts_at" type="datetime-local" class="rounded-xl border p-3"><input name="ends_at" type="datetime-local" class="rounded-xl border p-3"><button class="rounded-xl bg-blue-600 p-3 font-bold text-white">Create campaign</button></form><div class="mt-8 space-y-3">@foreach($campaigns as $c)<div class="rounded-xl border bg-white p-5"><b>{{$c->name}}</b> · {{$c->type}} · {{$c->value}}</div>@endforeach</div>{{$campaigns->links()}}</section>@endsection
