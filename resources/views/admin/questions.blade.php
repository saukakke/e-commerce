@extends('layouts.app') @section('title','Product questions — Admin') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Product questions</h1><div class="mt-8 space-y-3">@foreach($questions as $q)<form method="post" action="{{route('admin.questions.update',$q)}}" class="rounded-2xl border bg-white p-5">@csrf @method('PATCH')<b>{{$q->product->name}}</b><p>{{$q->question}}</p><textarea name="answer" class="mt-2 w-full rounded-lg border p-2">{{$q->answer}}</textarea><label class="mt-2 block"><input type="checkbox" name="approved" value="1" @checked($q->approved)> Approved</label><button class="mt-2 rounded-lg bg-slate-900 px-4 py-2 font-bold text-white">Save</button></form>@endforeach</div>{{$questions->links()}}</section>@endsection
