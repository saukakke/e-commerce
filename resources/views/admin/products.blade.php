@extends('layouts.app') @section('title','Manage products — Shoply') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><div class="flex justify-between"><h1 class="text-4xl font-black">Products</h1><div class="flex gap-3"><a href="{{route('admin.categories')}}" class="font-bold text-blue-600">Categories</a><a href="{{route('admin.dashboard')}}" class="font-bold text-blue-600">Dashboard</a></div></div><form method="post" action="{{route('admin.products.store')}}" class="mt-8 grid gap-3 rounded-2xl border bg-white p-6 md:grid-cols-4">@csrf<input name="name" placeholder="Product name" class="rounded-xl border px-3 py-2"><input name="sku" placeholder="SKU" class="rounded-xl border px-3 py-2"><select name="category_id" class="rounded-xl border px-3 py-2"><option value="">Category</option>@foreach($categories as $c)<option value="{{$c->id}}">{{$c->name}}</option>@endforeach</select><input name="price" type="number" step="0.01" placeholder="Price" class="rounded-xl border px-3 py-2"><input name="discount_price" type="number" step="0.01" placeholder="Discount price" class="rounded-xl border px-3 py-2"><input name="stock" type="number" placeholder="Stock" class="rounded-xl border px-3 py-2"><input name="image" placeholder="Image URL" class="rounded-xl border px-3 py-2 md:col-span-2"><textarea name="description" placeholder="Description" class="rounded-xl border px-3 py-2 md:col-span-3"></textarea><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="featured" value="1"> Featured</label><button class="rounded-xl bg-blue-600 px-4 py-2 font-bold text-white">Create product</button></form><div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Actions</th></tr></thead><tbody>@foreach($products as $p)<tr class="border-t"><td class="p-4"><b>{{$p->name}}</b><div class="text-xs text-slate-400">{{$p->sku}}</div></td><td>{{$p->category?->name}}</td><td>₦{{number_format($p->sale_price,2)}}</td><td>{{$p->stock}}</td><td class="p-3"><details><summary class="cursor-pointer font-bold text-blue-600">Edit</summary><form method="post" action="{{route('admin.products.update',$p)}}" class="mt-3 grid gap-2">@csrf @method('PUT')<input name="name" value="{{$p->name}}" class="rounded-lg border px-2 py-1"><input name="sku" value="{{$p->sku}}" class="rounded-lg border px-2 py-1"><input name="price" value="{{$p->price}}" type="number" step="0.01" class="rounded-lg border px-2 py-1"><input name="discount_price" value="{{$p->discount_price}}" type="number" step="0.01" class="rounded-lg border px-2 py-1"><input name="stock" value="{{$p->stock}}" type="number" class="rounded-lg border px-2 py-1"><select name="category_id" class="rounded-lg border px-2 py-1"><option value="">No category</option>@foreach($categories as $c)<option value="{{$c->id}}" @selected($p->category_id===$c->id)>{{$c->name}}</option>@endforeach</select><input name="image" value="{{$p->image}}" class="rounded-lg border px-2 py-1"><textarea name="description" class="rounded-lg border px-2 py-1">{{$p->description}}</textarea><label><input type="checkbox" name="featured" value="1" @checked($p->featured)> Featured</label><label><input type="checkbox" name="status" value="1" @checked($p->status)> Active</label><button class="rounded-lg bg-slate-900 px-3 py-2 font-bold text-white">Save</button></form><form method="post" action="{{route('admin.products.delete',$p)}}" class="mt-2">@csrf @method('DELETE')<button class="font-bold text-red-600">Delete</button></form></details></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{$products->links()}}</div></section>@endsection
