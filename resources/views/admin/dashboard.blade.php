@extends('layouts.app') @section('title','Admin dashboard — Shoply') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><div class="flex items-end justify-between"><div><p class="text-sm font-bold uppercase tracking-widest text-blue-600">Operations</p><h1 class="mt-2 text-4xl font-black">Admin dashboard</h1></div><div class="flex gap-2"><a href="{{route('admin.products')}}" class="rounded-xl border bg-white px-4 py-2 text-sm font-bold">Products</a><a href="{{route('admin.orders')}}" class="rounded-xl border bg-white px-4 py-2 text-sm font-bold">Orders</a></div></div><div class="mt-8 grid grid-cols-2 gap-4 lg:grid-cols-4">@foreach($stats as $label=>$value)<div class="rounded-2xl border bg-white p-6"><div class="text-sm capitalize text-slate-500">{{$label}}</div><div class="mt-2 text-3xl font-black">{{$label==='revenue'?'₦'.number_format($value,2):number_format($value)}}</div></div>@endforeach</div><div class="mt-8 rounded-2xl border bg-white p-6"><div class="flex justify-between"><h2 class="text-xl font-black">Latest orders</h2><a href="{{route('admin.orders')}}" class="text-sm font-bold text-blue-600">View all</a></div><div class="mt-5 space-y-2">@foreach($orders as $order)<div class="flex justify-between border-b py-3 text-sm"><span>#{{$order->number}} · {{$order->status_label}}</span><b>₦{{number_format($order->total,2)}}</b></div>@endforeach</div></div></section>@endsection
