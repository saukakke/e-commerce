@extends('layouts.app') @section('title','Gift cards — Admin') @section('content')<section class="mx-auto max-w-6xl px-4 py-12"><h1 class="text-4xl font-black">Gift cards</h1><form method="post" action="{{route('admin.giftcards.store')}}" class="mt-8 flex gap-3 rounded-2xl border bg-white p-6">@csrf<input name="amount" type="number" step="0.01" placeholder="Value" class="flex-1 rounded-xl border p-3"><input name="expires_at" type="date" class="rounded-xl border p-3"><button class="rounded-xl bg-blue-600 px-5 font-bold text-white">Issue</button></form><div class="mt-8 space-y-3">@foreach($cards as $c)<div class="rounded-xl border bg-white p-5"><b>{{$c->code}}</b> · ₦{{number_format($c->balance,2)}} · {{$c->active?'Active':'Inactive'}}</div>@endforeach</div>{{$cards->links()}}</section>@endsection
