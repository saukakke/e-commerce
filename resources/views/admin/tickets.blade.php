@extends('layouts.app') @section('title','Support tickets — Admin') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Support tickets</h1><div class="mt-8 space-y-4">@foreach($tickets as $t)<form method="post" action="{{route('admin.tickets.update',$t)}}" class="rounded-2xl border bg-white p-6">@csrf @method('PATCH')<b>{{$t->subject}}</b><span class="ml-2 text-sm">{{$t->user->email}}</span><p class="my-3">{{$t->message}}</p><div class="grid gap-3 md:grid-cols-2"><select name="status" class="rounded-lg border p-2">@foreach(['open','in_progress','resolved','closed'] as $s)<option @selected($t->status===$s)>{{$s}}</option>@endforeach</select><textarea name="admin_reply" placeholder="Reply" class="rounded-lg border p-2">{{$t->admin_reply}}</textarea></div><button class="mt-3 rounded-lg bg-slate-900 px-4 py-2 text-white">Update</button></form>@endforeach</div>{{$tickets->links()}}</section>@endsection
