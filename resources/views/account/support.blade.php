@extends('layouts.app') @section('title','Support — Shoply') @section('content')<section class="mx-auto max-w-5xl px-4 py-12"><h1 class="text-4xl font-black">Customer support</h1><form method="post" action="{{route('support.store')}}" class="mt-8 space-y-3 rounded-2xl border bg-white p-6">@csrf<input name="subject" placeholder="Subject" class="w-full rounded-xl border p-3"><select name="priority" class="rounded-xl border p-3"><option>normal</option><option>low</option><option>high</option></select><textarea name="message" rows="5" placeholder="How can we help?" class="w-full rounded-xl border p-3"></textarea><button class="rounded-xl bg-blue-600 p-3 font-bold text-white">Submit ticket</button></form><div class="mt-8 space-y-3">@foreach($tickets as $t)<article class="rounded-xl border bg-white p-5"><b>{{$t->subject}}</b><span class="ml-2 text-xs">{{$t->status}}</span><p>{{$t->message}}</p>@if($t->admin_reply)<div class="mt-3 rounded-lg bg-slate-50 p-3">{{$t->admin_reply}}</div>@endif</article>@endforeach</div></section>@endsection
