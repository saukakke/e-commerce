@extends('layouts.app') @section('title','My orders — Shoply') @section('content')<section class="mx-auto max-w-5xl px-4 py-12"><h1 class="text-4xl font-black">My orders</h1><div class="mt-8 space-y-3">@foreach($orders as $order)<a href="{{route('orders.show',$order)}}" class="flex items-center justify-between rounded-2xl border bg-white p-5"><div><b>#{{$order->number}}</b><p class="mt-1 text-sm text-slate-500">{{$order->created_at->format('d M Y')}} · {{$order->status_label}}</p></div><b>₦{{number_format($order->total,2)}}</b></a>@endforeach</div><div class="mt-8">{{$orders->links()}}</div></section>@endsection
