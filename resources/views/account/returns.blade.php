@extends('layouts.app') @section('title','Returns — Shoply') @section('content')<section class="mx-auto max-w-5xl px-4 py-12"><h1 class="text-4xl font-black">Returns & refunds</h1><form method="post" action="{{route('returns.store')}}" class="mt-8 grid gap-3 rounded-2xl border bg-white p-6 md:grid-cols-2">@csrf<select name="order_id" class="rounded-xl border p-3">@foreach($orders as $o)<option value="{{$o->id}}">{{$o->number}} — ₦{{number_format($o->total,2)}}</option>@endforeach</select><input name="reason" placeholder="Reason" class="rounded-xl border p-3"><textarea name="description" placeholder="Describe the issue" class="rounded-xl border p-3 md:col-span-2"></textarea><button class="rounded-xl bg-blue-600 p-3 font-bold text-white md:col-span-2">Request return</button></form><div class="mt-8 space-y-3">@foreach($returns as $r)<div class="rounded-xl border bg-white p-5"><b>{{$r->order->number}}</b> · {{$r->status}}<p class="text-sm text-slate-500">{{$r->reason}}</p></div>@endforeach</div></section>@endsection
