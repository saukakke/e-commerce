@extends('layouts.app') @section('title','Wishlist — Shoply') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-4xl font-black">Wishlist</h1><div class="mt-8 grid grid-cols-2 gap-5 md:grid-cols-4">@forelse($items as $item)@php($product=$item->product)<div class="overflow-hidden rounded-2xl border bg-white"><a href="{{route('products.show',$product)}}"><img src="{{$product->image}}" class="h-56 w-full object-cover"></a><div class="p-4"><a href="{{route('products.show',$product)}}" class="font-bold">{{$product->name}}</a><p class="mt-2 font-black">₦{{number_format($product->sale_price,2)}}</p><div class="mt-3 flex gap-2"><form method="post" action="{{route('cart.add',$product)}}">@csrf<button class="rounded-xl bg-blue-600 px-3 py-2 text-xs font-bold text-white">Add to cart</button></form><form method="post" action="{{route('wishlist.toggle',$product)}}">@csrf<button class="rounded-xl border px-3 py-2 text-xs font-bold">Remove</button></form></div></div></div>@empty<p class="col-span-full text-slate-500">Your wishlist is empty.</p>@endforelse</div><div class="mt-8">{{$items->links()}}</div></section>@endsection
