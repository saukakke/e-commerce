@extends('layouts.app') @section('title','My account — Shoply') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><div class="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p class="text-sm font-bold uppercase tracking-widest text-blue-600">Account</p><h1 class="mt-2 text-4xl font-black">Hello, {{auth()->user()->name}}</h1></div><div class="flex gap-2"><a href="{{route('orders')}}" class="rounded-xl border bg-white px-4 py-2 text-sm font-bold">Orders</a><a href="{{route('wishlist')}}" class="rounded-xl border bg-white px-4 py-2 text-sm font-bold">Wishlist</a>@if(auth()->user()->isAdmin())<a href="{{route('admin.dashboard')}}" class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white">Admin</a>@endif<form method="post" action="{{route('logout')}}">@csrf<button class="rounded-xl border px-4 py-2 text-sm font-bold">Sign out</button></form></div></div><div class="mt-10 grid gap-8 lg:grid-cols-2"><div class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-black">Recent orders</h2><div class="mt-5 space-y-3">@forelse($orders as $order)<a href="{{route('orders.show',$order)}}" class="flex justify-between rounded-xl bg-slate-50 p-4"><span><b>#{{$order->number}}</b><br><small class="text-slate-500">{{$order->status_label}}</small></span><b>₦{{number_format($order->total,2)}}</b></a>@empty<p class="text-slate-500">No orders yet.</p>@endforelse</div></div><div class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-black">Saved addresses</h2><div class="mt-5 space-y-3">@foreach($addresses as $address)<div class="rounded-xl bg-slate-50 p-4"><div class="flex justify-between"><b>{{$address->label}}</b><form method="post" action="{{route('account.addresses.delete',$address)}}">@csrf @method('DELETE')<button class="text-xs font-bold text-red-600">Remove</button></form></div><p class="mt-1 text-sm text-slate-600">{{$address->name}} · {{$address->phone}}<br>{{$address->address}}, {{$address->city}}, {{$address->state}}</p></div>@endforeach</div><form method="post" action="{{route('account.addresses.store')}}" class="mt-5 grid gap-3">@csrf<input name="label" placeholder="Label e.g. Home" class="rounded-xl border px-3 py-2"><input name="name" placeholder="Recipient name" class="rounded-xl border px-3 py-2"><input name="phone" placeholder="Phone" class="rounded-xl border px-3 py-2"><input name="address" placeholder="Address" class="rounded-xl border px-3 py-2"><div class="grid grid-cols-2 gap-3"><input name="city" placeholder="City" class="rounded-xl border px-3 py-2"><input name="state" placeholder="State" class="rounded-xl border px-3 py-2"></div><button class="rounded-xl bg-slate-900 py-2 font-bold text-white">Save address</button></form></div></div></section>@endsection
