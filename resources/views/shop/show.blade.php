@extends('layouts.app')
@section('content')<div class="mx-auto max-w-7xl px-4 py-12"><div class="grid gap-10 md:grid-cols-2"><div class="overflow-hidden rounded-3xl bg-white"><img src="{{ $product->image }}" alt="{{ $product->name }}" class="h-[30rem] w-full object-cover"></div><div class="py-4"><p class="font-semibold text-blue-600">{{ $product->category?->name }}</p><h1 class="mt-2 text-4xl font-black">{{ $product->name }}</h1><p class="mt-5 text-3xl font-black">₦{{ number_format($product->sale_price,2) }}</p>@if($product->discount_price)<p class="mt-1 text-slate-400 line-through">₦{{ number_format($product->price,2) }}</p>@endif<p class="mt-6 leading-7 text-slate-600">{{ $product->description }}</p><p class="mt-6 text-sm font-semibold">{{ $product->stock }} units available</p><form action="{{ route('cart.add',$product) }}" method="POST" class="mt-6 flex gap-3">@csrf<input type="number" name="quantity" value="1" min="1" max="{{ $product->stock }}" class="w-24 rounded-xl border px-4 py-3"><button class="flex-1 rounded-xl bg-slate-900 px-6 py-3 font-bold text-white">Add to cart</button></form></div></div>@if($related->count())<h2 class="mt-20 text-3xl font-black">You may also like</h2><div class="mt-6 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">@foreach($related as $item)<a href="{{ route('products.show',$item) }}" class="rounded-2xl border bg-white p-3"><img src="{{ $item->image }}" class="h-48 w-full rounded-xl object-cover"><p class="mt-3 font-bold">{{ $item->name }}</p><p class="font-black">₦{{ number_format($item->sale_price,2) }}</p></a>@endforeach</div>@endif</div>@endsection