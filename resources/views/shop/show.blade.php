@extends('layouts.app') @section('title',$product->name.' — Shoply') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><div class="grid gap-12 md:grid-cols-2"><div class="overflow-hidden rounded-3xl bg-white"><img src="{{$product->image}}" class="h-[520px] w-full object-cover" alt="{{$product->name}}"></div><div class="py-4"><p class="text-sm font-bold uppercase tracking-widest text-blue-600">{{$product->category?->name}}</p><h1 class="mt-3 text-4xl font-black">{{$product->name}}</h1><div class="mt-4 flex items-center gap-3"><span class="text-2xl font-black">₦{{number_format($product->sale_price,2)}}</span>@if($product->discount_price)<span class="text-slate-400 line-through">₦{{number_format($product->price,2)}}</span>@endif</div><p class="mt-6 leading-7 text-slate-600">{{$product->description}}</p><div class="mt-6 rounded-xl bg-slate-100 p-4 text-sm">{{$product->stock}} units currently available</div><div class="mt-6 flex gap-3"><form method="post" action="{{route('cart.add',$product)}}" class="flex gap-2">@csrf<input type="number" name="quantity" value="1" min="1" max="{{$product->stock}}" class="w-20 rounded-xl border px-3"><button class="rounded-xl bg-blue-600 px-7 py-3 font-bold text-white">Add to cart</button></form>@auth<form method="post" action="{{route('wishlist.toggle',$product)}}">@csrf<button class="rounded-xl border bg-white px-5 py-3 font-bold">♡ Wishlist</button></form>@endauth</div></div></div><div class="mt-16 border-t pt-10"><h2 class="text-2xl font-black">Customer reviews</h2><div class="mt-6 space-y-4">@forelse($product->reviews as $review)<div class="rounded-2xl border bg-white p-5"><div class="font-bold">{{$review->user->name}} · {{$review->rating}}/5</div><p class="mt-2 text-slate-600">{{$review->comment}}</p></div>@empty<p class="text-slate-500">No reviews yet.</p>@endforelse</div>@auth<div class="mt-8 rounded-2xl border bg-white p-6"><h3 class="font-bold">Leave a review</h3><form method="post" action="{{route('reviews.store',$product)}}" class="mt-4 grid gap-3">@csrf<select name="rating" class="rounded-xl border px-4 py-3"><option value="5">5 — Excellent</option><option value="4">4 — Good</option><option value="3">3 — Average</option><option value="2">2 — Poor</option><option value="1">1 — Bad</option></select><textarea name="comment" rows="4" class="rounded-xl border px-4 py-3" placeholder="Share your experience"></textarea><button class="w-fit rounded-xl bg-slate-900 px-5 py-3 font-bold text-white">Submit review</button></form></div>@endauth</div></section>@endsection
