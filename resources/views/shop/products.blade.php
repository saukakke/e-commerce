@extends('layouts.app') @section('title','Shop — Shoply') @section('content')<section class="mx-auto max-w-7xl px-4 py-12"><div class="flex flex-col gap-6 md:flex-row md:items-end md:justify-between"><div><p class="text-sm font-bold uppercase tracking-widest text-blue-600">Catalog</p><h1 class="mt-2 text-4xl font-black">Shop all products</h1></div><form class="flex gap-2" method="get"><input name="q" value="{{request('q')}}" placeholder="Search products..." class="w-full rounded-xl border bg-white px-4 py-3 md:w-72"><button class="rounded-xl bg-slate-900 px-5 font-bold text-white">Search</button></form></div><div class="mt-8 flex flex-wrap gap-2">@foreach($categories as $category)<a href="{{route('products',['category'=>$category->slug])}}" class="rounded-full border bg-white px-4 py-2 text-sm font-semibold">{{$category->name}}</a>@endforeach<a href="{{route('products')}}" class="rounded-full bg-blue-50 px-4 py-2 text-sm font-semibold text-blue-700">All</a></div><div class="mt-10 grid grid-cols-2 gap-5 md:grid-cols-3 lg:grid-cols-4">@forelse($products as $product)<div class="overflow-hidden rounded-2xl border bg-white"><a href="{{route('products.show',$product)}}"><img src="{{$product->image}}" class="h-64 w-full object-cover" alt="{{$product->name}}"></a><div class="p-4"><div class="text-xs text-slate-400">{{$product->category?->name}}</div><a href="{{route('products.show',$product)}}" class="mt-1 block font-bold">{{$product->name}}</a><div class="mt-3 flex items-center justify-between"><span class="font-black">₦{{number_format($product->sale_price,2)}}</span><form method="post" action="{{route('cart.add',$product)}}">@csrf<button class="rounded-full bg-blue-600 px-3 py-2 text-xs font-bold text-white">Add</button></form></div></div></div>@empty<div class="col-span-full rounded-2xl border bg-white p-12 text-center text-slate-500">No products match your search.</div>@endforelse</div><div class="mt-10">{{$products->links()}}</div></section>@endsection
