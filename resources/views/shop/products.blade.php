@extends('layouts.app')
@section('content')<div class="mx-auto max-w-7xl px-4 py-12"><div class="flex flex-col gap-6 md:flex-row md:items-end md:justify-between"><div><p class="font-semibold text-blue-600">Catalog</p><h1 class="text-4xl font-black">Shop products</h1></div><form class="flex gap-2" method="GET"><input name="q" value="{{ request('q') }}" placeholder="Search products..." class="w-full rounded-xl border bg-white px-4 py-3 md:w-80"><button class="rounded-xl bg-slate-900 px-5 py-3 font-bold text-white">Search</button></form></div><div class="mt-8 flex flex-wrap gap-2">@foreach($categories as $category)<a href="{{ route('products',['category'=>$category->slug]) }}" class="rounded-full border bg-white px-4 py-2 text-sm">{{ $category->name }}</a>@endforeach<a href="{{ route('products') }}" class="rounded-full border bg-white px-4 py-2 text-sm">All</a></div><div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">@forelse($products as $product)<article class="overflow-hidden rounded-2xl border bg-white"><a href="{{ route('products.show',$product) }}"><img src="{{ $product->image }}" alt="{{ $product->name }}" class="h-56 w-full object-cover"></a><div class="p-5"><p class="text-xs font-semibold uppercase text-slate-500">{{ $product->category?->name }}</p><h2 class="mt-1 font-bold">{{ $product->name }}</h2><p class="mt-2 font-black">₦{{ number_format($product->sale_price,2) }}</p><form action="{{ route('cart.add',$product) }}" method="POST">@csrf<button class="mt-4 w-full rounded-xl bg-slate-900 px-4 py-3 font-bold text-white">Add to cart</button></form></div></article>@empty<p class="col-span-full py-16 text-center text-slate-500">No products found.</p>@endforelse</div><div class="mt-10">{{ $products->links() }}</div></div>@endsection