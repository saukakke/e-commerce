@extends('layouts.app') @section('title','Compare products — Shoply') @section('content')<section class="mx-auto max-w-6xl px-4 py-12"><h1 class="text-4xl font-black">Compare products</h1><div class="mt-8 grid gap-5 md:grid-cols-3">@forelse($products as $p)<article class="rounded-2xl border bg-white p-5"><img src="{{$p->image}}" class="h-48 w-full rounded-xl object-cover"><h2 class="mt-4 font-black">{{$p->name}}</h2><p class="mt-2">₦{{number_format($p->sale_price,2)}}</p><p class="mt-2 text-sm text-slate-500">{{$p->description}}</p><a href="{{route('products.show',$p->slug)}}" class="mt-4 inline-block font-bold text-blue-600">View product</a></article>@empty<p>No products selected for comparison.</p>@endforelse</div></section>@endsection
